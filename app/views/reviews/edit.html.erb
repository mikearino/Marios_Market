<h1>Edit review:</h1>
<div class="forms">

<%= form_for [@product, @review] do |f| %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :content_body %>
  <%= f.text_area :content_body %>
  <p><%= f.submit %></p>
<% end %>
</div>
